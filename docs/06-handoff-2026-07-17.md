# 交接卡：AI Project Reader 明天从这里继续

时间：2026-07-17 00:17

## 一句话状态

今天已经把 `AI Project Reader` 从 v0.1 推到 v0.2：

```text
v0.1：手动输入项目资料 -> 生成结构化项目阅读报告
v0.2：输入本地项目路径 -> 自动扫描项目 -> 基于扫描上下文回答问题
```

项目地址：

- GitHub：https://github.com/axianxianxian/ai-project-reader-learning
- 本地路径：`/Users/luckyxian/Documents/Codex/2026-07-05/gu/ai-project-reader-learning`

## 今天已经完成

1. 建好了 GitHub 仓库并推送代码。
2. 建好了 Spring Boot 项目骨架。
3. 跑通了主链路：

```text
Controller
-> Service
-> PromptBuilder
-> AiGateway
-> MockAiGateway
-> ProjectReadingReport
```

4. 新增了本地项目扫描能力：

```text
LocalProjectScanner
-> ProjectContext
```

5. 新增了基于本地项目上下文问答的链路：

```text
用户问题
-> 扫描本地项目
-> 组织 ProjectContext
-> 生成问答 prompt
-> MockAiGateway
-> ProjectQuestionAnswer
```

6. 测试已通过：

```bash
mvn test
```

## 你今天已经理解到的东西

你已经能说出这条主线：

> 用户请求先进入 Controller，Controller 调 Service。Service 负责编排流程，会让 PromptBuilder 组织给 AI 的提示词，再调用 AiGateway。现在还没有真实 AI，所以用 MockAiGateway 假装模型返回。最后结果被整理成结构化报告，再返回给用户。

这个理解是对的。

明天继续时，只要把几个词说准：

- `Controller`：接口入口
- `Service`：业务编排
- `PromptBuilder`：组织 prompt
- `AiGateway`：AI 调用边界
- `MockAiGateway`：假的 AI 实现，用来先跑通链路
- `ProjectReadingReport`：结构化项目阅读报告
- `LocalProjectScanner`：读取本地项目资料的工具
- `ProjectContext`：扫描后的项目上下文

## 明天先看这 3 个文件

只看这 3 个，不要被项目文件淹没。

1. `src/main/java/com/axian/projectreader/app/ProjectReadingService.java`

看它怎么把分析、扫描、问答三条流程串起来。

2. `src/main/java/com/axian/projectreader/app/LocalProjectScanner.java`

看它怎么读取 README、pom、目录树和关键 Java 文件。

3. `src/main/java/com/axian/projectreader/ai/ProjectReadingPromptBuilder.java`

看它怎么把项目上下文变成 prompt。

## 明天最小任务

明天只做一个验收：

> 用这个项目分析它自己，并看懂 `/scan` 和 `/ask` 的返回结果。

推荐顺序：

1. 启动项目。
2. 调 `/api/project-reading/scan`。
3. 看返回的 `ProjectContext`。
4. 调 `/api/project-reading/ask`。
5. 看返回的 `ProjectQuestionAnswer`。
6. 用自己的话说出：它到底扫到了什么，它又怎么拿这些上下文回答问题。

## 明天可以问自己的 5 个问题

1. `LocalProjectScanner` 是不是就是一个 tool？
2. `ProjectContext` 和 prompt 是什么关系？
3. 为什么问答准不准，先取决于上下文准不准？
4. 如果它没读到某个关键文件，会发生什么？
5. 如果接入真实 Spring AI，应该替换哪一层？

## 下一步代码方向

明天不要急着做 RAG。

下一步最适合做的是：

```text
把 /ask 的 mock 回答变得更像真实代码问答
```

也就是让 `MockAiGateway` 根据问题和扫描到的文件，返回更具体的：

- 短答案
- 相关文件
- 逻辑链路
- 缺失上下文
- 下一步阅读动作

再下一步才是接 Spring AI。

## 作品集讲法

这段可以先背下来：

> 这是一个 AI 项目阅读助手。我先没有急着接真实模型，而是把 AI 应用的工程链路拆清楚：Controller 接请求，Service 编排流程，PromptBuilder 组织提示词，AiGateway 隔离模型调用，结构化结果返回给业务。v0.2 我加入了 LocalProjectScanner，让系统可以读取本地项目的 README、构建文件、目录树和关键 Java 文件，再基于这些上下文回答问题。这是从普通 AI 接口调用走向自动读码 agent 的第一步。

## 明天别做的事

- 不要同时学 Spring AI、RAG、agent、测试、前端。
- 不要去搜一堆文章把自己淹没。
- 不要急着追求“像 Cursor 那样自动读全部代码”。

先把这条链路吃透：

```text
本地项目路径
-> 扫描上下文
-> 组织 prompt
-> 结构化问答
```

吃透这个，再往 Spring AI 接。
